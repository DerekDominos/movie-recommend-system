function reform() {
  var genres = genresData;
  var counts = countsData;
    let myCan = document.getElementById("pieChart");  // 获取canvas ID
    let ctx = myCan.getContext("2d");  // 获取绘图上下文（环境）
    let radius = 150 ;   // 半径
    let [cx,cy] = [myCan.width/2, myCan.height/2];  // 圆心坐标
    // 计算总销售额
    let totalcounts = 0;
    for(let i=0; i<=data.length-1; i++){
        totalcounts += data[i].counts ;
    }
    // 绘图
    let start = -Math.PI/2 ;   // 默认开始弧度
    for(let i=0; i<=data.length-1; i++){
        // 结束弧度： start+当前所占的弧度
        let end =start + data[i].counts/totalcounts*2*Math.PI ;
        // 画饼图
        ctx.beginPath();
        ctx.moveTo(cx,cy);
        ctx.arc(cx,cy,radius,start, end );  // 画弧
        ctx.fill();
        // 下一个start 是这次的 end。
        start = end ;
}